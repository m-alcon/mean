import { Character } from "./character";
import { Category } from "./category"

export class Quote {
    text: string
    character: Character
    category: Category
    category_id: number
    character_id: number
    id: number
}